export interface Agendamento {
    nome: string;
    cpf: string;
    data: string;  // Formato 'YYYY-MM-DD'
    hora: string;  // Formato 'HH:mm:ss'
    servico: string;
    contato: string;
    local: string;
  }